# 声连 · Vision Pro 版 使用说明

> 目标：手机用豆包语音说话 → 点一下「发送」→ 文字自动进 Vision Pro 剪贴板 → 在 Vision Pro 粘贴。
>
> 原理：手机网页把文字**复制到 iPhone 剪贴板** → **通用剪贴板**同步到 Vision Pro → 系统粘贴。
>
> **不需要开发者账号、不需要配网、不需要快捷指令。**

## 一、一次性准备

1. 两台设备登录**同一个 Apple ID**；
2. 都打开「**接力 / Handoff**」（iPhone：设置 → 通用 → 隔空播放与接力）；
3. 蓝牙、Wi‑Fi 都打开，两台设备靠近一点。

## 二、日常使用

1. iPhone 用 Safari 打开：**https://blackorwhite12.github.io/phonevoice/vp.html**
   （建议：分享 → **添加到主屏幕**，以后一键打开。）
2. 点一下输入框（iOS 不允许网页自动弹键盘）→ 用**豆包输入法**语音说话。
3. 点页面上的「**✈️ 发送（复制到剪贴板）**」
   → 网页**当场复制**，**页面不跳走、键盘不掉**。
4. 到 **Vision Pro** 输入框：**长捏 → 粘贴**。
5. 回到本页**接着说话**即可（不用重新点框）。

## 三、为什么能"不丢键盘"

因为整个动作**全程不离开 Safari**。对比：走「快捷指令」会跳到别的 App，键盘必然掉——这就是之前版本的毛病。

## 四、系统限制（绕不过，共 3 下）

| 动作 | 原因 |
|---|---|
| 点一下输入框 | iOS 不允许网页自动弹出键盘 |
| 点「发送」 | iOS 要求"复制到剪贴板"必须在用户点击里触发 |
| Vision Pro 里粘贴 | visionOS 不允许 App 往别的 App 注入文字，只能系统粘贴 |

## 五、排错

- **Vision Pro 粘不出来**：确认同一 Apple ID、已开接力、蓝牙/Wi‑Fi 已开；复制后**等 3–5 秒**再粘；两台放近。
- **点「发送」没反应**：网页会自动退回"快捷指令"方式（如果手机里有名为「声连」的快捷指令）。
- **网页打不开**（国内偶尔）：可临时改用 Mac 局域网提供该页面。

## 六、文件与相关

- 网页源文件：`docs/vp.html`（部署在 GitHub Pages）
- 「声连」快捷指令：**现在用不到了**，仅作为"网页复制失败"时的兜底，可以不管它。
- 原生 visionOS App（方案②，可免掉手机上的点击）：代码已写好并在模拟器跑通，等 macOS/Xcode 升级后装真机。
