# 声连 · Vision Pro 版（方案①）使用说明

> 目标：手机用豆包语音说话 → 说「发送」→ 自动复制 → **在 Vision Pro 里粘贴**。
> 原理：iPhone「快捷指令」复制 → 「通用剪贴板」同步到 Vision Pro → 系统粘贴。
> 优点：**不需要开发者账号、不需要配对、不需要中转**；代价：手机上要点 2 下。

## 一、一次性准备

1. **两台设备登录同一个 Apple ID**；都打开「**接力 / Handoff**」；蓝牙和 Wi‑Fi 都打开；两台靠近。
2. iPhone「快捷指令」里新建一个快捷指令，命名 **`声连`**，**只保留 2 个动作**：
   1. **拷贝到剪贴板** —— 内容选「**快捷指令输入**」；
   2. **打开 App** —— 选「**Safari浏览器**」（用于发送后自动回到输入页）。

   > ⚠️ 重要：**不要**保留「停止并输出」这个动作。
   > 它会立刻结束快捷指令，导致后面的「打开 App」不执行，Safari 就不会自动回来。
   >
   > （可有一个「接收输入」动作，设成「如果没有输入：继续」。）

## 二、日常使用

1. iPhone 用 Safari 打开：**https://blackorwhite12.github.io/phonevoice/vp.html**
   （建议：分享 → 添加到主屏幕，以后一键打开。）
2. 点一下输入框（iOS 不允许网页自动弹键盘）→ 切换到**豆包输入法**语音说话。
3. 说一句「**发送**」→ 弹出「在'快捷指令'中打开？」→ 点「**打开**」。
4. 文字自动复制 → 通用剪贴板 → 到 **Vision Pro**：输入框上**长捏 → 粘贴**。
5. 手机自动回到输入页（光标已自动聚焦），继续下一句即可。

## 三、为什么必须点这几下（系统限制，绕不过）

| 动作 | 原因 |
|---|---|
| 手机点一下输入框 | iOS 不允许网页自动弹出键盘 |
| 手机点「打开」 | Safari 对"网页调起快捷指令"强制要一次确认 |
| Vision Pro 里粘贴 | visionOS 不允许 App 往别的 App 注入文字，只能走系统粘贴 |

想彻底去掉手机上那两下（说「发送」就直接进剪贴板、Vision Pro 只点一次粘贴）→ 需要做**原生 visionOS App（方案②）**。

## 四、排错

- **发完不自动回到 Safari**：检查「声连」快捷指令里是否还留着「**停止并输出**」，删掉它。
- **Vision Pro 粘不出来**：确认同一 Apple ID、已开接力、蓝牙/Wi‑Fi 已开；拷贝后**等 3–5 秒**再粘贴；两台设备放近。
- **粘出来的是旧内容**：通用剪贴板有延迟，重试一次。
- **网页打不开**（国内偶尔）：可临时改用 Mac 局域网提供该页面。

## 五、文件位置

- 网页源文件：`docs/vp.html`（部署在 GitHub Pages：`https://blackorwhite12.github.io/phonevoice/vp.html`）
